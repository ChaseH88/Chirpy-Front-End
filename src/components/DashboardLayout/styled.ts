import { Box } from "@mui/material";
import styled from "styled-components";

export const Container = styled(Box)`
  padding: 2em;
  margin: 0 auto;
  max-width: 95%;
  min-width: 500px;
  margin: 20px auto;
`;
